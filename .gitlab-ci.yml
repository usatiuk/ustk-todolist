image: node:10

stages:
  - testBackend
  

cache:
  paths:
  - node_modules/
  - client/node_modules

testBackend:
  stage: test
  services:
    - mongo:4
  script:
    - npm i npm@latest -g
    - npm i
    - npm test
